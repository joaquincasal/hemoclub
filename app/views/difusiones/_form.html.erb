<%= form_with(model: difusion) do |form| %>
  <% if difusion.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(difusion.errors.count, "error") %> prohibited this difusion from being saved:</h2>

      <ul>
        <% difusion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre, class: "label" %>
    <div class="control">
      <%= form.text_field :nombre, class: "input is-rounded" %>
    </div>
  </div>

  <div class="field">
    <%= form.label :lista_dinamica_id, "Lista", class: "label" %>
    <div class="control">
      <div class="select is-rounded">
        <%= form.collection_select :lista_dinamica_id, ListaDinamica.all, :id, :nombre, { include_blank: "" } %>
      </div>
    </div>
  </div>

  <div class="field">
    <%= form.label :plantilla_id, "Plantilla", class: "label" %>
    <div class="control">
      <div class="select is-rounded">
        <%= form.collection_select :plantilla_id, Plantilla.all, :id, :nombre, { include_blank: "" } %>
      </div>
    </div>
  </div>

  <div>
    <%= form.button type: "submit", class: "button is-primary is-rounded is-pulled-right mt-4" do %>
        <span class="icon">
          <i class="fas fa-floppy-disk"></i>
        </span>
      <span>Guardar</span>
    <% end %>
  </div>
<% end %>
