<!DOCTYPE html>
<html data-theme="light">
  <head>
    <title>Myapp2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
  <header>
    <section class="hero is-small is-primary">
      <div class="hero-body">
        <p class="subtitle">
          FHBA MKT
        </p>
      </div>
    </section>
    <nav class="navbar" role="navigation" aria-label="main navigation">
      <div class="navbar-brand">
        <a class="navbar-item" href="#">
          <%= image_tag("logo.png") %>
        </a>
      </div>

      <div id="navbarBasicExample" class="navbar-menu">
        <div class="navbar-start">
          <a class="navbar-item">
            Donantes
          </a>
          <a class="navbar-item">
            Donaciones
          </a>
        </div>

        <div class="navbar-end">
          <div class="navbar-item">
            <div class="buttons">
              <% if usuario_signed_in? %>
                <%= button_to "Cerrar sesión", destroy_usuario_session_path, method: :delete, class: "button is-primary is-rounded is-outlined is-light" %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </nav>
  </header>
  <main>
    <section class="section">
      <div class="container">
        <% if notice %>
          <div class="notification is-success" data-controller="hello">
            <button class="delete" data-action="click->hello#delete"></button>
            <%= notice %>
          </div>
        <% end %>
        <% if alert %>
          <div class="notification is-danger" data-controller="hello">
            <button class="delete" data-action="click->hello#delete"></button>
            <%= alert %>
          </div>
        <% end %>
        <%= yield %>
      </div>
    </section>
  </main>
  <footer class="footer">
    <p>Este es el footer</p>
  </footer>

  </body>
</html>
